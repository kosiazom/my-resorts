import React from 'react'
import Hero from '../components/Hero'

const Rooms = () => {
    return <Hero hero="roomsHero" /> 
    //this will overwrite the default Hero we already set up
}
export default Rooms
